import { Meta, Story } from '@storybook/react'

import { AddButton } from '.'

export default {
  title: 'AddButton',
  component: AddButton
} as Meta

export const Default: Story = () => <AddButton />
